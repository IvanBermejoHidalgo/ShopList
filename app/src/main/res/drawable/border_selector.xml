<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el campo de texto no está enfocado -->
    <item android:state_focused="false" android:color="@color/white" />
    <!-- Por defecto, cuando el campo de texto está enfocado -->
    <item android:color="@color/colorsplash" />
</selector>
